export const apiUrl = "https://chatbot.avayes.com/api";

export const global_header = {
  Accept: "application/json",
};

// export const API_DOWNLOAD = (id) => `${apiUrl}/camera/download/?camera_id=${id}`;
